/** @type {import('next').NextConfig} */
const nextConfig = { reactStrictMode: true,
    images: {
      domains: ['images.unsplash.com'], // Añade el dominio permitido aquí};
    },
};
export default nextConfig;
